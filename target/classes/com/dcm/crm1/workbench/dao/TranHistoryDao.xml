<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dcm.crm1.workbench.dao.TranHistoryDao">
    <insert id="addTranHistory">
        insert into tbl_tran_history
        (
        id,
        tranId,
        money,
        expectedDate,
        stage,
        createBy,
        createTime
        )
        values
        (
        #{id},
        #{tranId},
        #{money},
        #{expectedDate},
        #{stage},
        #{createBy},
        #{createTime}




        )
    </insert>


    <select id="showHistory" resultType="TranHistory">
        select * from tbl_tran_history where tranId=#{tranId} order by createTime desc
    </select>

  	
</mapper>

































